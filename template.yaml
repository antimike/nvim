---
name: $(basename "$url")
url: $(req url)
category: $(req -a category)
priority: $(def -i priority 0)
stars: $(opt -i stars)
configs: $(def -a configs $NONE)
description: $(opt description)
requires: $(def -a requires $NONE)
---
